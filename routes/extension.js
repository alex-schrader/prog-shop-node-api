const Router = require('express-promise-router')
const db = require('../db')
const router = new Router()

module.exports = router
router.get('/', async (req, res, next) => {
    // TODO: finish this route. Use helper functions from utils
    // const url = req.query.url
    // res.send(details)
})
